# Project handoff

**Updated:** 2026-09-03 22:30 PDT
**Session:** Claude Code · e1dd0df8-3f9d-4a99-8425-1502f553264c · backup armed, retype made reversible, restart.sh false-ERROR fixed
**Status:** Tree clean on `regen-prod`, **17 commits ahead of origin (unpushed — a local guard blocks the push, see Next steps #1)**. The nightly backup was **never bootstrapped into launchd**; it is now armed and its first run is in flight. `/entities/retype` captured no reversal and had already made **142 irreversible merges**; it now captures one. `restart.sh` reported ERROR on restarts that had succeeded. Focused suite **119 passed / 1 failed**; the 1 is `test_canonical_entity_types` red on another session's `Incident`/`Component` inserts, deliberately left red (it is a decision, not a defect).

> ## ⚠ START KOI SESSIONS IN THIS CHECKOUT
>
> `~/projects/koi-processor-service` — despite the name, this is **not a separate repo**. All four
> local checkouts are clones of `gaiaaiagent/koi-processor`; the directory names encode *roles*, not
> repositories. There is no `gaiaaiagent/koi-processor-service`.
>
> | checkout | role | branch |
> |---|---|---|
> | `koi-processor-service` | **serves :8351** (uvicorn cwd) — start here | `regen-prod` |
> | `koi-processor-runtime` | the sensor launchd jobs | `regen-prod`, never switch |
> | `RegenAI/koi-processor` | shared dev checkout | whatever a session left it on |
>
> **Do not start ontology sessions in `RegenAI/koi-processor`.** It is a moving target sessions
> branch-switch freely, with no `PROJECT_HANDOFF.md` of its own — a session there starts oriented to
> whatever is at `~/projects/RegenAI/PROJECT_HANDOFF.md` instead, which is a different project.

## Completed this session

- **The nightly backup was never armed.** `launchctl print gui/501/com.personal-koi.backup` → *"Could not find service"*, while 13 sibling `com.personal-koi.*` jobs were loaded. The plist was written 2026-09-01 21:40 and never bootstrapped, so the 03:15 fires on Sep 2 and Sep 3 never happened and the newest dump was **Aug 31, hand-run** — ~3 days unbacked on a 27 GB database. The tell was free and unread: the two log paths the plist declares did not exist. Now bootstrapped, `03:15` interval confirmed, first run in flight (12.18 GB expected, ~2h; the script's own header documents that duration). The prior session's headline "Backups exist and restore" was true about the *restore* and never true about the *schedule*.
- **`/entities/retype` is reversible** (`d06b038`). `capture_reversal()` had exactly one production call site — the `/merge` route — while `/retype` calls the same `_do_merge` helper and captured nothing. **142 retype merges already exist with `reversal IS NULL`** and are permanently unrecoverable. The capture went into `_do_retype`, not `_do_merge`, because three of four branches do something `_do_merge` cannot see: in-place performs no merge, mint **creates** the survivor, resurrect revives a tombstone. The mint case is why "does it still bind?" is not a sufficient test — `unmerge` restored the loser and never deleted the survivor, leaving two live rows sharing a `normalized_text`, both embedded, both competing in exact/fuzzy/ANN. `REVERSAL_SCHEMA` → 2 but `{1,2}` are both accepted; bumping alone would have orphaned the 57 existing captures.
- **The launchd guard enumerated a subset** (`4941b1b`). `PLIST_GLOBS` covered only the two `com.personal*` namespaces, so `com.darren.claude-session-sensor` — which writes `session_chunks` and loads from `RegenAI/koi-sensors` — was invisible while the suite reported green over 17 plists, in a file whose own comment names that exact failure. Widening it found a **second** violation reached via a launched script rather than a plist.
- **`restart.sh` cried wolf** (`606e4aa`). Its wait was 30 iterations of `curl --max-time 4; sleep 1`; against a refused port curl fails in microseconds, so the budget was ~30s wall clock while startup under a concurrent `pg_dump` takes **40–73s** (measured three times). It printed ERROR and exited 1 on restarts that had succeeded. Now polls to a wall-clock deadline and distinguishes *failure to start* / *crash loop* / *alive but slow*. Committed under `scripts/` and byte-compared against the installed copy — `~/.config/personal-koi` scripts have no repo, no test and no review, which is why this survived.
- **Retracted a false claim before it shipped** (`812a1e3`). "The Concept-absorption tasks resolve as a side effect of generating the prompt enum (audit D4b)" is wrong: the extractor already emits `Protocol` 336 times, so the enum was never the constraint. Corrected in `PROJECT_HANDOFF.md` and in the task-context string `c3869af` writes onto every rolling class task — which is **not yet deployed**, so zero live tasks carry the bad text. It still stands in `c3869af`'s commit message (history).

## Next steps

1. **Publishing is blocked by a LOCAL guard, not by the remote.** 17 commits here, 2 in `~/projects/RegenAI/koi-sensors`. An unattended-git guard refuses any remote that is not the OpenCivics-Labs CIE repo with a `darren|codex|claude/<branch>` ref, and it does not prompt, so a session cannot satisfy it. `regen-prod` itself has no PR requirement (verified 2026-08-14). The operator runs:
   `git -C ~/projects/koi-processor-service push origin regen-prod`
   `git -C ~/projects/RegenAI/koi-sensors push origin main`
   The second matters most: `f5c7f88` (empty-turn-pair chunk fix) exists on **one disk**, in a checkout sessions branch-switch freely. The guard over-matches on the substring — it also refused a read-only `ls .git/hooks` (because of `pre-push`) and a Python heredoc that ran no git command at all.
2. **Pull `koi-processor-runtime`** (40 behind origin / 53 behind local HEAD; dirty is `?? venv` only). **This is the flood fix** — `c3869af` collapses 974 per-document type-mismatch tasks into six rolling class tasks, and it has never run because the daily 07:45 `substack-deep-extract` job executes from that clone. Verify the file with `grep -c 'doc-ingest-typemismatch-class-'`, then verify the **jobs**: wait ~10 min and confirm `com.personal-koi.embedding-repair` (300s) has fired — **by log mtime and `logs/embedding-repair-state.json`, never by exit code**, which that job returns 0 for by design when the provider is down. Do **not** `git clean -fd`; nine jobs execute from the untracked `venv/`.
3. **NUC cutover**: memo at `~/.claude/plans/nuc-koi-reconciliation-refresh-2026-08-31.md`, parked pending operator go. Its named prerequisite **PR #43 is `CONFLICTING` and 180 commits behind** — a rebase plus a three-way adjudication, its own session.
4. *(structural)* **Ontology generator (audit D4b)** — generate the extraction prompt enum from `allowed_entity_types`. ⚠ **The "resolves the 518 Concept-absorption tasks as a side effect" half was FALSE and is retracted** (see Completed). Still worth doing on its own merits: the vocabulary has **nine in-repo copies plus three out-of-repo**, mutually disagreeing, and **nothing in production reads `allowed_entity_types`** — one repo-wide grep hit, inside a string. Blocked on the decisions below.

## Open questions

- **Are `Protocol`/`Project` TYPES or FACETS? This gates migration 113.** Registered as koi task `koi-infra-entity-vocabulary-types-vs-facets-gates-113` (id 9315, due 2026-09-17). `allowed_facets` already exists with an FK into `allowed_entity_types` and a shape CHECK — zero rows, zero users. Facets ⇒ the 176 retypes vanish. Types ⇒ 176 URI changes, each cascading to a rid mapping and a vault note.
- **Migration 113 was never written, and its gate has been open ~10 days.** Migration 111's header says enforcement is 113's job, gated on the non-canonical population emptying. It **is** empty — `live_rows = 0` across all 22 unregistered types; the 156 remaining are tombstones. Two blockers before writing it: (a) a committed architecture doc, `docs/architecture/migration-112-entity-type-canonicalization.md`, argues enforcement should **not** be a hard constraint — overridable, its own supporting number is wrong, but explicitly; (b) `personal_koi_test.allowed_entity_types` has **0 rows** and **111 is missing from that DB**, so an FK there would refuse every entity insert in the suite. Note also that a NOT VALID FK installs triggers on the *referenced* table, so afterwards a vocabulary row with entities behind it can no longer be deleted — which means `Incident`/`Component` must be settled first, not after.
- **`Incident`/`Component`** were inserted by psql by session `cf2ea77e` at 19:58 and now have live rows behind them. That is what turned `test_canonical_entity_types` red. **Left red deliberately** — picking a side silently would foreclose the decision. Their lane recommends adding them to `DEFAULT_SCHEMAS`; task `koi-infra-entity-type-conformance-incident-component`.
- **A third vocabulary surface, same disease.** `allowed_predicates` is 56 rows, all lowercase, FK-enforced on `entity_relationships` (10,663 rows, all lowercase). `knowledge_facts` has **no FK** and holds 60,344 rows across **4,926 distinct UPPERCASE** predicates. Two disjoint namespaces. Consequence: `knowledge_router.py:1017` compares `fact.predicate.upper()` against the as-stored value while the INSERT at `:1095` stores it raw, so writing facts with the *declared* (lowercase) vocabulary silently disables both duplicate-skip and supersession. The first 68 such rows landed 2026-09-03. Decide which namespace is canonical alongside the type question.
- **`/register-entity` and `visibility_scope`: populated is not conformant.** 34,093/34,093 rows are non-null — because the column is `NOT NULL DEFAULT 'unclassified'`. Only **8 rows in 34,093** ever carried a non-default value, and the endpoint writes the column to `entity_rid_mappings` only, never to `entity_registry`. There is no `properties` column at all. Separately, `'personal'` is **rejected** by the CHECK (`public|team|confidential|unclassified`).
- **142 retype merges are permanently unrecoverable.** Fixed going forward; those rows predate the capture and `unmerge` refuses them by design rather than guessing.
- The three prior open questions (Organization→Person experiment, 17 unseedable do-not-merge rows, `KOI_CLAIMS_SERVICE_TOKEN`) are unchanged from 2026-09-02.

## Verification and working tree

- **Branch/status:** `regen-prod`, working tree clean, **17 ahead** of `origin/regen-prod`, 0 behind.
- **Tests:** `119 passed / 1 failed` across the touched suites. The 1 is `test_canonical_entity_types::test_the_code_defaults_and_the_database_allowlist_agree` — red on `only_db = ['Component','Incident']`, caused by another session's psql inserts and left red on purpose. `test_merge_reversal` 18 passed; `test_launchd_job_targets` 71 passed / 2 skipped.
- **Positive controls run, not merely written.** Disabling only the minted-survivor delete makes the retype mint round trip fail with both `concept-*` and `person-*` live, while twin and in-place still pass. Emptying `KNOWN_DEV_CHECKOUT_EXCEPTIONS` fails exactly the two known launchd violations and nothing else. Appending one line to `scripts/restart.sh` fails the new drift test. `restart.sh` was exercised against the live slow-start reproducer (healthy after 73s) and a forced `KOI_RESTART_TIMEOUT=1` proved the alive-but-slow message.
- **Live:** service PID from the 22:18 restart — **postdates the 21:50 edits**, so tonight's fix is actually loaded. Healthy, embeddings available. Backup `pg_dump` in flight since 21:41:58.
- **⚠ Route presence does not prove module vintage.** `/entities/unmerge` showed PRESENT in `/openapi.json` while the serving process predated `merge_reversal.py` by 85 seconds — routes register from `admin_router`. Compare `ps -o lstart` against the **mtime** of the newest loaded source file, not the commit time; Python loads from disk, and a commit touching no files leaves mtime old. Use `launchctl list` or `lsof -ti :8351 -sTCP:LISTEN` — plain `lsof -ti :8351 | head -1` returned a transient *client* PID tonight and briefly read as a restart that had not happened.

## Recent sessions

| Date | Provider | Session | Summary |
|---|---|---|---|
| 2026-08-24/25 | Claude Code | b289ac1e | Executed the full prior plan: 9 sweep fixes, Meeting-notes repair (270 notes), migration 112 complete (421→0). Established `koi-sensors-runtime` + hardened the launchd guard. |
| 2026-08-26 | Claude Code | b289ac1e (resumes) | 3rd conflict wave cleaned; built `com.personal-koi.vault-conflict-sweep`; fixed a real `ThrottleInterval < StartInterval` bug + added a mutation-tested anti-storm pin; fixed `test_koi_flow_integration.py`'s months-stale collection failure. |
| 2026-08-26 | Claude Code | c1defaa8 | **Verification pass.** Found A7 unexecutable, a gating inconsistency, A3's live blast radius; caught the conflict cleanup incomplete at 101 files; named the iCloud root cause; canary-proved the sweep fires unattended. No code changes. |
| 2026-09-01/02 | Claude Code | 72cf052b | **Phase 0/1 hardening.** Backup + verified restore; merge reversibility (`unmerge`, used on 57 merges); `entity_non_match` seeded (44) and enforcing at 6 tiers; credential + persona guards; `:8351` LAN hole closed and A/B-verified; type-mismatch void closed. 12 commits. |
| 2026-09-03 | Claude Code | e1dd0df8 | **Backup armed; retype made reversible.** The nightly backup plist had never been bootstrapped — newest dump was Aug 31, hand-run, ~3 days unbacked on 27 GB. `/entities/retype` captured no reversal and had already made **142 irreversible merges**. Launchd guard enumerated a subset (missed `com.darren.*`, found 2 real violations). `restart.sh` reported ERROR on restarts that succeeded (30s budget vs 40–73s startup). Retracted the false D4b claim before it shipped. 5 commits, all positive-controlled. |
