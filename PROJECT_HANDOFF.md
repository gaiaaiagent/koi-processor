# Project handoff

**Updated:** 2026-09-04 13:05 PDT
**Session:** Claude Code · 1e1f2abb-70f1-4639-9612-cd74c6cde36d · decisions 9315/9317 prepared and double-audited; email guard completed; 116 cleared
**Status:** Tree clean on `regen-prod` @ `9f55e66`, **1 commit ahead of origin (unpublished)**. `koi-sensors` is **2 ahead of origin/main, unpublished** (`aad40dd`, `d5dde85`). Decisions **9315** and **9317** are PREPARED, not decided — full evidence pack at `~/.claude/plans/koi-vocabulary-decisions-9315-9317-2026-09-04.md`, attached to both koi tasks. **Migration 116 is NOT blocked** and applies cleanly today (verified). The email From-name guard is now complete at all three call sites. A `claude_sessions` venv has been built in `koi-sensors-runtime`, which is one of the two remaining conditions for retiring the dev-checkout exemption.

> ## ⚠ START KOI SESSIONS IN THIS CHECKOUT
>
> `~/projects/koi-processor-service` — despite the name, this is **not a separate repo**. All four
> local checkouts are clones of `gaiaaiagent/koi-processor`; the directory names encode *roles*, not
> repositories. There is no `gaiaaiagent/koi-processor-service`.
>
> | checkout | role | branch |
> |---|---|---|
> | `koi-processor-service` | **serves :8351** (uvicorn cwd) — start here | `regen-prod` |
> | `koi-processor-runtime` | the sensor launchd jobs | `regen-prod`, never switch |
> | `RegenAI/koi-processor` | shared dev checkout | whatever a session left it on |
>
> **Do not start ontology sessions in `RegenAI/koi-processor`.** It is a moving target sessions
> branch-switch freely, with no `PROJECT_HANDOFF.md` of its own — a session there starts oriented to
> whatever is at `~/projects/RegenAI/PROJECT_HANDOFF.md` instead, which is a different project.

## Completed this session

- **Decisions 9315 + 9317 prepared, not decided.** Two workflows (20 agents, ~3.2M tokens): 7 evidence lenses, each independently re-derived by a refute-by-default auditor, then a completeness critic, then a second audit of the numbers that RANK the options — because the critic correctly found those were the least-scrutinised figures in the run. **Every lens came back CORRECTED.** Pack: `~/.claude/plans/koi-vocabulary-decisions-9315-9317-2026-09-04.md`; both koi tasks now carry it in `context`.
- **Five corrections that change the decisions.** (1) The disputed population is **166, not 176** — 176 is `88+88` from a prose comment, two classes overlapping on 10 entities, and is not reproducible under any variant. (2) The **27.9× cost ratio is ~8×** — it is an entity-count ratio quoted on a row-count table; rewired rows are 41,084/5,156 = **7.97×** (9.61× with unhandled columns). Direction survives, magnitude does not. (3) **All 142 historical retypes are irreversible — 0%, not 71%**: the 57 reversal-bearing rows are `service:claims-service` merges from 2026-09-02, not retypes. (4) **`knowledge_facts` has TWO live write paths**, not one — `domain_event_handlers.py:770 → :590 → :808`, invisible to a literal grep, 439 federation applies, no dedup and no validation. Re-running that method refutes "one create path" for `entity_registry` (29 writer files) and `entity_relationships` (17). (5) **The NUC peer IS reachable** (both memos claimed otherwise with no failed command) — and its vocabulary divergence has **already crossed the wire without failing**, because `entity_registry`/`knowledge_facts` carry no FK.
- **Migration 116 is not blocked — the briefing's premise was false, and so was my first reading of it.** `com.darren.claude-session-sensor` runs from the shared **dev** checkout, not `koi-sensors-runtime`, and that checkout is at `f5c7f88`. PID 5944 started 2026-09-02 13:44:35, **27m36s after** the fixed file's mtime; **zero** stubs written since, while the producer stayed live (520 chunks today, 0 stubs). Repair population re-counted: **291,078**, not 116's stated 294,937 — it *shrinks*, because the sensor DELETEs a session's chunks before rewriting. I originally reported 116 as blocked by inferring from the runtime clone's branch state: reading the layout instead of asking the process, which is the exact trap the topology section warns about.
- **The email From-name guard is complete** (`aad40dd`, koi-sensors). `368e759`'s message claims "BOTH call sites"; there are three, and `proton_sensor.py:407` was untouched. A defect-class sweep with a passing positive control confirms the class is exactly three and there is no fourth. Measured by replaying the real corpus through both predicates: 63 → 61 distinct names accepted. **Honest scope:** the guard is weak against the junk that dominates — over the 931 live email-sourced Person rows it rejects **39 (4.2%)**; its skip-list anchors at string start, so `Team at Read` and `Kyle @ Pinata` pass.
- **`requirements.txt` could not build a working venv** (`d5dde85`). `claude_session_sensor.py` imports `requests` at `:34`; it was undeclared, and `openai` uses httpx so nothing pulled it in. Caught by a positive control (importing the module under the new venv), not by reading the file — installing the stated requirements and declaring success would have passed. The dev checkout's large shared venv carries `requests` for other reasons, so the gap was invisible there and would have surfaced on the repoint as stopped session ingestion.
- **Retention pruning now has a date.** The first unattended deletion this system will ever perform is **2026-09-09 ~03:15**, removing the Aug-31 dump. Scope is correctly tight — the glob matches only the 3 auto dumps; all 6 historical/hand-made dumps fall outside it. Weekly tagging is correct (`date +%u`, 7=Sunday) and first fires **Sunday 2026-09-06**, ahead of the first prune. ⚠ **The loop has no floor:** it deletes every ≥8-day-old auto dump after any single successful run, so a long dormancy followed by one fire collapses N backups to 1 — and this job has already proven it can sit silently dormant.

## Next steps

**Everything below is gated on ONE operator action: publishing `koi-sensors` (2 commits).** A local
unattended-git guard declines to act on any remote that is not the OpenCivics-Labs CIE repo and does
not prompt, so a session cannot do it. Nothing is blocked at the remote.

Operator decisions taken 2026-09-04: close the 116 fragility *before* applying it, and on the sensor
side pull **and kickstart all three jobs** rather than letting the timer carry it.

**Ordered sequence. Starting anywhere else reinstates a bug.**

1. **Publish** `koi-sensors` `f5c7f88..d5dde85` (`aad40dd` third call site, `d5dde85` requirements).
2. **Pull** `git -C ~/projects/koi-sensors-runtime pull` → `0b5584e..d5dde85`. This is the step that
   deploys the email guard to all three sites at once. ⚠ Do **not** `git clean -fd` — the jobs run
   from untracked venvs, and a freshly built `venv/` now sits at the repo root.
3. **Kickstart the three email jobs.** A bare pull reaches only `email-sensor` (StartInterval 1800,
   observed 40–65 min in practice). `proton-email-sensor` (PID 870) and `email-watcher` (PID 871)
   are KeepAlive daemons ~8 days old that import `EmailSensor` at module load and hold stale code
   until kickstarted. Verify each with `ps -p <PID> -o lstart=` against source mtime — never
   `/openapi.json`, never the directory layout.
4. **Repoint** `com.darren.claude-session-sensor` at `koi-sensors-runtime`. The venv is **already
   built and positive-controlled** (the module imports cleanly under it; asyncpg 0.31.0 / openai
   2.20.0 pinned to match the running job). ⚠ The plist names the dev checkout in **five** places,
   not two: `PATH`, the interpreter, the script, the config, and `WorkingDirectory`. Two `.bak`
   copies already exist in `~/Library/LaunchAgents`. **This must come after step 2** — repointing at
   `0b5584e` reinstates the empty-turn-pair stub producer.
5. **Apply migration 116 to BOTH** `personal_koi` and `personal_koi_test`. Verified applicable:
   0 violating rows written since 2026-09-02 13:07:59.714772, positive control 291,078 pre-boundary.
   `NOT VALID`, so no rewrite and no scan. The test DB has an empty `session_chunks`, so it is free
   there. Standing watch-item: 115 was applied only to the live DB and left 19 tests red.
6. **Retire** the `KNOWN_DEV_CHECKOUT_EXCEPTIONS` entry in `tests/test_launchd_job_targets.py` and
   run that suite. As of now only **one** of its three retirement conditions holds (published);
   step 2 satisfies the second and step 4 the third.
7. **Do NOT** run step 3 of migration 116 (the historical stub deletion) as part of this. It needs a
   REINDEX of a 3,745 MB index and a fresh re-count — the population moved 3,859 rows in two days
   and is self-healing via the sensor's per-session DELETE.

**Also open, unchanged:** NUC cutover (memo parked; PR #43 `CONFLICTING`, 180 behind); the ontology
generator (audit D4b), blocked on 9315/9317; Dependabot task 9323 (JS surface, due 2026-10-01).

## Open questions

- **Are `Protocol`/`Project` TYPES or FACETS? This gates migration 113.** Registered as koi task `koi-infra-entity-vocabulary-types-vs-facets-gates-113` (id 9315, due 2026-09-17). `allowed_facets` already exists with an FK into `allowed_entity_types` and a shape CHECK — zero rows. ⚠ **'zero users' is FALSE at the database layer:** `entity_facets_registered_guard` is bound to the **enabled** trigger `tr_entity_facets_registered` on `entity_registry`'s hot write path, and with the table empty it **rejects every non-empty facet write today**. Accurate statement: zero application readers, one live database reader. ⚠ **CORRECTED 2026-09-04: the population is 166, not 176, and only 5 of the 166 have a rid mapping or a vault note — not 'each'.** The real cascade is 2,965 distinct `knowledge_facts` rows and 2,125 `document_entity_links`. The facets branch is ~8× the types branch by rewired rows (not 27.9×), and that cost is forced ONLY if 113 lands as a strict FK — under a warn-only 113 it is zero.
- **Migration 113 was never written, and its gate has been open ~10 days.** Migration 111's header says enforcement is 113's job, gated on the non-canonical population emptying. It **is** empty — `live_rows = 0` across all 22 unregistered types; the 156 remaining are tombstones. Two blockers before writing it: (a) a committed architecture doc, `docs/architecture/migration-112-entity-type-canonicalization.md`, argues enforcement should **not** be a hard constraint — overridable, its own supporting number is wrong, but explicitly; (b) ⚠ **CORRECTED 2026-09-04: 111's DDL IS present in `personal_koi_test`** — the tables, the validated FK, `entity_facets`, `resolution_tier`, the trigger and both CHECKs. What is missing is only the **seed rows (0)** and the ledger row. So a constraining 113 would **apply cleanly there and then reject every entity insert** — failure at test time, not apply time — and seeding that one table clears it without applying 111. `allowed_predicates` is also 0 rows there, so **one seeding operation clears the blocker for BOTH 9315 and 9317**; note `064_claims_engine.sql` is missing from the commonly-cited ten-migration seed list. Note also that a NOT VALID FK installs triggers on the *referenced* table, so afterwards a vocabulary row with entities behind it can no longer be deleted — which means `Incident`/`Component` must be settled first, not after.
- **`Incident`/`Component`** were inserted by psql by session `cf2ea77e` at 19:58 and now have live rows behind them. That is what turned `test_canonical_entity_types` red. **Left red deliberately** — picking a side silently would foreclose the decision. Their lane recommends adding them to `DEFAULT_SCHEMAS`; task `koi-infra-entity-type-conformance-incident-component`.
- **A third vocabulary surface — LIVE, and two orders of magnitude wider than the entity-type one.** Registered as koi task `koi-infra-predicate-vocabulary-what-is-it-gates-fk-vs-warn` (id 9317, due 2026-09-17); decide **alongside** the types-vs-facets question, not separately. `allowed_predicates` is 56 rows, all lowercase, FK-enforced on **two** tables (`entity_relationships` 10,663 rows, and `pending_relationships`). `knowledge_facts` has **no FK**: 60,412 rows, **4,927 distinct predicates**. **This is not a casing artifact** — case-folded against the allowlist, only **34 of 4,927** match, so ~4,893 have no entry in any casing and the allowlist covers **0.69%** of live vocabulary. Contrast the entity-type problem, which is *smaller*: there the non-canonical population was entirely tombstones (`live_rows = 0`). Consequence already biting: `knowledge_router.py:1017` compares `fact.predicate.upper()` against the as-stored value while `:1095` stores it raw, so writing facts with the **declared** (lowercase) vocabulary silently disables both duplicate-skip and supersession — first 68 such rows landed 2026-09-03. Do **not** patch the `.upper()` in isolation; normalising case without deciding which namespace is canonical just makes the two disagree more quietly. ⚠ **RETRACTED 2026-09-04 — do NOT use `predicate_raw` as evidence.** An earlier version of this bullet said to measure only on rows carrying it. That column **cannot produce evidence**: the single INSERT at `api/routers/knowledge_router.py:1089` binds *both* `predicate` (`$3`) and `predicate_raw` (`$11`) to the same expression, `fact.predicate`. They cannot differ. "0 rows diverge" is satisfiable by construction — the population grew 458 → 2,476 overnight and divergence stayed exactly 0. I ran that query, got the zero, and read it as "nothing normalises today" without asking why it could never be otherwise. **The admissible evidence is the write path** — but ⚠ **CORRECTED 2026-09-04: there are TWO live insert paths, not one.** The second is `api/domain_event_handlers.py:770` `_insert_fact` → `_insert_with_drift_retry` → the dynamic `INSERT INTO {table}` at `:590`, called with the literal `"knowledge_facts"` at `:808`. It is live (439 federation applies), does no dedup or supersession, writes no `predicate_raw` and validates nothing — and it is **invisible to `grep 'INSERT INTO knowledge_facts'`**, which is exactly how the one-path conclusion was reached. Any option costed as "gate the one insert path" is under-costed. The router path stores `fact.predicate` untransformed; the `.upper()` at `:1017` feeds only the dedup comparison and the supersession-policy lookup, never the stored value. That is what establishes the 4,893 unlisted predicates as verbatim extractor output. Also: `canonicalize_predicate` (`api/ontology_registry.py:127`) has **zero callers** — the same built-and-unwired shape as `canonicalize_entity_type_checked`.
- **`/register-entity` and `visibility_scope`: populated is not conformant.** 34,093/34,093 rows are non-null — because the column is `NOT NULL DEFAULT 'unclassified'`. Only **8 rows in 34,093** ever carried a non-default value, and the endpoint writes the column to `entity_rid_mappings` only, never to `entity_registry`. There is no `properties` column at all. Separately, `'personal'` is **rejected** by the CHECK (`public|team|confidential|unclassified`).
- **29 open Dependabot alerts** on `gaiaaiagent/koi-processor` (11 high — `form-data`, `axios`), surfaced by the remote at publication. These are the **JS/TS** surface, not the Python backend serving `:8351`. Untriaged; a blind `npm audit fix` is the wrong move because the count is against the repo's DEFAULT branch, which is not necessarily `regen-prod`. Task `koi-infra-dependabot-29-open-alerts-js-surface` (id 9323, due 2026-10-01).
- **142 retype merges are permanently unrecoverable.** Fixed going forward; those rows predate the capture and `unmerge` refuses them by design rather than guessing.
- The three prior open questions (Organization→Person experiment, 17 unseedable do-not-merge rows, `KOI_CLAIMS_SERVICE_TOKEN`) are unchanged from 2026-09-02.

## Verification and working tree

- **Branch/status:** `regen-prod` @ `b5a7e8e`, working tree clean, **0 ahead / 0 behind** — published. (Counted AFTER the handoff commit — it does not count itself. The previous session wrote 12 where it was 13 for the same reason; assume any ahead-count written *in* the handoff is one low unless it was re-measured after committing.)
- **Tests:** `119 passed / 1 failed` across the touched suites. The 1 is `test_canonical_entity_types::test_the_code_defaults_and_the_database_allowlist_agree` — red on `only_db = ['Component','Incident']`, caused by another session's psql inserts and left red on purpose. `test_merge_reversal` 18 passed; `test_launchd_job_targets` 71 passed / 2 skipped.
- **Positive controls run, not merely written.** Disabling only the minted-survivor delete makes the retype mint round trip fail with both `concept-*` and `person-*` live, while twin and in-place still pass. Emptying `KNOWN_DEV_CHECKOUT_EXCEPTIONS` fails exactly the two known launchd violations and nothing else. Appending one line to `scripts/restart.sh` fails the new drift test. `restart.sh` was exercised against the live slow-start reproducer (healthy after 73s) and a forced `KOI_RESTART_TIMEOUT=1` proved the alive-but-slow message.
- **Live:** service PID from the 22:18 restart — **postdates the 21:50 edits**, so tonight's fix is actually loaded. Healthy, embeddings available. Backup `pg_dump` in flight since 21:41:58.
- **⚠ Route presence does not prove module vintage.** `/entities/unmerge` showed PRESENT in `/openapi.json` while the serving process predated `merge_reversal.py` by 85 seconds — routes register from `admin_router`. Compare `ps -o lstart` against the **mtime** of the newest loaded source file, not the commit time; Python loads from disk, and a commit touching no files leaves mtime old. Use `launchctl list` or `lsof -ti :8351 -sTCP:LISTEN` — plain `lsof -ti :8351 | head -1` returned a transient *client* PID tonight and briefly read as a restart that had not happened.

## Recent sessions

| Date | Provider | Session | Summary |
|---|---|---|---|
| 2026-08-24/25 | Claude Code | b289ac1e | Executed the full prior plan: 9 sweep fixes, Meeting-notes repair (270 notes), migration 112 complete (421→0). Established `koi-sensors-runtime` + hardened the launchd guard. |
| 2026-08-26 | Claude Code | b289ac1e (resumes) | 3rd conflict wave cleaned; built `com.personal-koi.vault-conflict-sweep`; fixed a real `ThrottleInterval < StartInterval` bug + added a mutation-tested anti-storm pin; fixed `test_koi_flow_integration.py`'s months-stale collection failure. |
| 2026-08-26 | Claude Code | c1defaa8 | **Verification pass.** Found A7 unexecutable, a gating inconsistency, A3's live blast radius; caught the conflict cleanup incomplete at 101 files; named the iCloud root cause; canary-proved the sweep fires unattended. No code changes. |
| 2026-09-01/02 | Claude Code | 72cf052b | **Phase 0/1 hardening.** Backup + verified restore; merge reversibility (`unmerge`, used on 57 merges); `entity_non_match` seeded (44) and enforcing at 6 tiers; credential + persona guards; `:8351` LAN hole closed and A/B-verified; type-mismatch void closed. 12 commits. |
| 2026-09-04 | Claude Code | 1e1f2abb | **Decisions 9315/9317 prepared; email guard completed; 116 cleared.** 20 agents over two workflows, every lens returned CORRECTED. Killed the 27.9× ratio (it is ~8×), the 176 population (166), the 142/57 reversibility split (0%, not 71%), the one-insert-path premise (two live writers), and "the NUC is unreachable" (it is reachable, and already holds the divergent vocabulary without failing). Corrected my own false report that 116 was blocked — I read the layout instead of asking the process. 2 koi-sensors commits, both positive-controlled. |
| 2026-09-03 | Claude Code | e1dd0df8 | **Backup armed; retype made reversible.** The nightly backup plist had never been bootstrapped — newest dump was Aug 31, hand-run, ~3 days unbacked on 27 GB. `/entities/retype` captured no reversal and had already made **142 irreversible merges**. Launchd guard enumerated a subset (missed `com.darren.*`, found 2 real violations). `restart.sh` reported ERROR on restarts that succeeded (30s budget vs 40–73s startup). Retracted the false D4b claim before it shipped. 5 commits, all positive-controlled. |
